import React from 'react';
import { Text } from 'react-native';
import { ErrorChartPropsType } from './types';

const ErrorChart: React.FC<ErrorChartPropsType> = () => {
  return <Text>error chart</Text>;
};

export default ErrorChart;
