import React from 'react';
import { Text } from 'react-native';
import { NoDataPropsType } from './types';

const NoData: React.FC<NoDataPropsType> = () => {
  return <Text>no data</Text>;
};

export default NoData;
